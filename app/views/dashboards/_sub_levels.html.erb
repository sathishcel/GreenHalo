<% unless level_wgus.blank? %>
    <% level_wgus.each do |wgu| %>

        <tr class="gradeX" id = "sub_wgu_<%= wgu.id %>" value = "<%= wgu.id %>">

          <td>
            <div class="company-img dropdown">
              <a class="dropdown-toggle" data-toggle="dropdown" href="#" style="font-size: 18px;">
                <i class="fa fa-wrench"></i>
              </a>

              <ul class="dropdown-menu dropdown-user">
                <li>
                  <a href="#"  role="menuitem" tabindex="-1" data-toggle="modal" data-target="#myModal_add_data"
                     title="Add Data">Add Data</a>
                </li>
                <li>
                  <a href="#"  role="menuitem" tabindex="-1" data-toggle="modal" data-target="#myModal_wgu_settings"
                     title="Settings">Settings</a>
                </li>
                <li>
                  <a href="#">Reports</a>
                </li>
                <li>
                  <a href="#">Share and Connect</a>
                </li>
              </ul>
            </div>

          </td>
          <td><b><%= wgu.name %></b> <br /> Street <br /> City <br /> State, Zip<br />ID#: <%= tracking_number(wgu.id) %></td>
          <td></td>
          <td><%= "#{wgu.id}" +'0000'%></td>
          <td class="center"><%= "#{wgu.id}" +'00'%></td>
          <td class="center"><%= "#{wgu.id}" +'0%'%></td>
          <td class="center"><%= "#{wgu.id}" +'000'%></td>
          <td class="center"><%= "#{wgu.id}" +'0'%></td>
          <td class="center"><%= "#{wgu.id}" +'0'%></td>
          <td class="center"><%= "#{wgu.id}" +'0'%></td>
          <td class="center"><%= "#{wgu.id}" +'0%'%></td>
          <td class="center"><%= "#{wgu.id}" +'0'%></td>
          <td class="center"><%= "#{wgu.id}" +'0'%></td>
        </tr>
<% end %>
<% end %>


<% unless sub_level.blank? %>
    <% sub_level.each do |level| %>
        <tr class="gradeA" id = "sub_level_<%= level.id %>" value = "<%= level.id %>" order = "not-open">
          <td style="text-align: center;">
            <div class="company-img dropdown">
              <span class="dropdown-toggle" data-toggle="dropdown" href="#" style="font-size: 18px;">
                <i class="fa fa-bars" style="cursor: pointer; font-size: 24px;"></i>
              </span>

              <ul class="dropdown-menu dropdown-user">
                <li role="presentation">
                  <a href="#"  role="menuitem" tabindex="-1" data-toggle="modal" data-target="#myModal_reports"
                     title="Reports">
                    <div>Reports</div>
                  </a>
                </li>

                <li role="presentation">
                  <a href="#"  role="menuitem" tabindex="-1" data-toggle="modal" data-target="#myModal15"
                     title="Program Settings">
                    <div>Program Settings</div>
                  </a>
                </li>
                <li role="presentation">
                  <a role="menuitem" tabindex="-1" href="#">
                    <div>Manage Portal</div>
                  </a>
                </li>

                <li role="presentation">

                  <%= link_to "Add Level", "wgu/#{level.id}/add_menu_level/#{level.id}", :remote => true,
                              :class => "add_next_level", :style => "color:#058F26;" %>
                </li>
                <li role="presentation">
                  <%= link_to "Add WGU", "wgu/#{level.id}/add_menu_level/#{level.id}", :remote => true,
                              :class => "add_level_to_wgu", :style => "color:#058F26;" %>
                </li>

                <li role="presentation">
                  <a role="menuitem" tabindex="-1" href="#" style="color:#058F26;">
                    Edit Profile
                  </a>
                </li>
              </ul>
            </div>

          </td>

          <td class = "sorting_1" ><b><%= level.name %></b> <br /> Street <br /> City<br /> State, Zip<br />ID#: <%= tracking_number(level.id) %></td>
          <td ><%= total_sub_level_count(level.id) %></td>
          <td ><%= "#{level.id}" +'0000'%></td>
          <td class="center"><%= "#{level.id}" +'00'%></td>
          <td class="center"><%= "#{level.id}" +'0%'%></td>
          <td class="center"><%= "#{level.id}" +'000'%></td>
          <td class="center"><%= "#{level.id}" +'0'%></td>
          <td class="center"><%= "#{level.id}" +'0'%></td>
          <td class="center"><%= "#{level.id}" +'0'%></td>
          <td class="center"><%= "#{level.id}" +'0%'%></td>
          <td class="center"><%= "#{level.id}" +'0'%></td>
          <td class="center"><%= "#{level.id}" +'0'%></td>
        </tr>
    <%end%>

<%end%>


<script>
    $(document).ready(function(){
        $(".gradeA").click(function(){
            var level_id = $(this).attr('value');
            var url = "/getting_sub_levels/" + level_id ;
            $.ajax({
                type: "GET",
                url: url
            });
        });


    });
</script>



<style>
    .wt-trho:hover{
        background-color: #cccccc !important;
    }
</style>